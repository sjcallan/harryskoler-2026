<?php

/*
|--------------------------------------------------------------------------
| Stateless API Routes
|--------------------------------------------------------------------------
|
| Public GET endpoints for the site's content live in `routes/web.php` so
| they are wrapped by session middleware. This gives controllers access to
| the authenticated user and the preview-mode session flag when deciding
| whether to include draft content.
|
| This file is intentionally left blank for now; add token-authenticated
| API routes here in the future if needed.
|
*/
